//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

/** Defines the types of blockchain events that can be subscribed to via a streaming provider. */
public enum TONStreamingWatchType {

    BALANCE("balance"),
    TRANSACTIONS("transactions"),
    JETTONS("jettons");

    /** Reads one of these from a wire value; for arrays of this enum. */
    public static final EnumParser<TONStreamingWatchType> VALUES = new EnumParser<TONStreamingWatchType>() {
        @Override
        public TONStreamingWatchType parse(Object wire) {
            return fromValue(wire);
        }
    };

    private final String value;

    TONStreamingWatchType(String value) {
        this.value = value;
    }

    public String getValue() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    /** The constant for a wire value, or null if it is absent or unknown. */
    public static TONStreamingWatchType fromValue(Object wire) {
        if (wire == null) {
            return null;
        }
        String text = wire.toString();
        for (TONStreamingWatchType candidate : values()) {
            if (candidate.value.equals(text)) {
                return candidate;
            }
        }
        return null;
    }
}
