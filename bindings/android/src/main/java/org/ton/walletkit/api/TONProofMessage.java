//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Message structure used for TON Connect proof of ownership. */
public class TONProofMessage {

    /** Builds one of these from JSON. */
    public static final Parser<TONProofMessage> PARSER = new Parser<TONProofMessage>() {
        @Override
        public TONProofMessage parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private TONProofMessageDomain domain;
    private String payload;
    private String stateInit;
    private Double timestamp;
    private Double workchain;

    /**
     * Domain information for the proof request
     * Required by the API.
     */
    public TONProofMessageDomain getDomain() {
        return domain;
    }

    public TONProofMessage setDomain(TONProofMessageDomain value) {
        this.domain = value;
        return this;
    }

    /**
     * Payload string to be signed
     * Required by the API.
     */
    public String getPayload() {
        return payload;
    }

    public TONProofMessage setPayload(String value) {
        this.payload = value;
        return this;
    }

    /**
     * Initial state of the wallet contract encoded in Base64
     * Required by the API.
     */
    public String getStateInit() {
        return stateInit;
    }

    public TONProofMessage setStateInit(String value) {
        this.stateInit = value;
        return this;
    }

    /**
     * Unix timestamp when the proof was created
     * Required by the API.
     */
    public Double getTimestamp() {
        return timestamp;
    }

    public TONProofMessage setTimestamp(Double value) {
        this.timestamp = value;
        return this;
    }

    /**
     * Workchain ID of the wallet address
     * Required by the API.
     */
    public Double getWorkchain() {
        return workchain;
    }

    public TONProofMessage setWorkchain(Double value) {
        this.workchain = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "domain", domain == null ? null : domain.toJson());
        TonApiJson.put(json, "payload", payload);
        TonApiJson.put(json, "stateInit", stateInit);
        TonApiJson.put(json, "timestamp", timestamp);
        TonApiJson.put(json, "workchain", workchain);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONProofMessage fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONProofMessage out = new TONProofMessage();
        out.domain = TONProofMessageDomain.fromJson(TonApiJson.optObject(json, "domain"));
        out.payload = TonApiJson.optString(json, "payload");
        out.stateInit = TonApiJson.optString(json, "stateInit");
        out.timestamp = TonApiJson.optDouble(json, "timestamp");
        out.workchain = TonApiJson.optDouble(json, "workchain");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
