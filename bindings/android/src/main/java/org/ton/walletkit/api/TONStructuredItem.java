//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/**
 * A union: {@code type} says which variant this is, and the matching
 * accessor returns it. An unrecognised type leaves the value as the raw
 * JSON rather than failing, so a new variant upstream does not break a
 * response that merely contains one.
 */
public class TONStructuredItem {

    public static final Parser<TONStructuredItem> PARSER = new Parser<TONStructuredItem>() {
        @Override
        public TONStructuredItem parse(JSONObject json) {
            return fromJson(json);
        }
    };

    public static final String TON = "ton";
    public static final String JETTON = "jetton";
    public static final String NFT = "nft";

    private String type;
    private Object value;

    /** Which variant this is; one of the constants above. */
    public String getType() {
        return type;
    }

    /** The variant, typed by {@code type}, or the raw JSON if unrecognised. */
    public Object getValue() {
        return value;
    }

    /** The value when {@code type} is {@code ton}, else null. */
    public TONTonTransferItem asTon() {
        return value instanceof TONTonTransferItem ? (TONTonTransferItem) value : null;
    }

    /** The value when {@code type} is {@code jetton}, else null. */
    public TONJettonTransferItem asJetton() {
        return value instanceof TONJettonTransferItem ? (TONJettonTransferItem) value : null;
    }

    /** The value when {@code type} is {@code nft}, else null. */
    public TONNftTransferItem asNft() {
        return value instanceof TONNftTransferItem ? (TONNftTransferItem) value : null;
    }

    public JSONObject toJson() {
        Object encoded = asJson(value);
        JSONObject json = encoded instanceof JSONObject ? (JSONObject) encoded : new JSONObject();
        TonApiJson.put(json, "type", type);
        return json;
    }

    private static Object asJson(Object value) {
        if (value instanceof TONTonTransferItem) {
            return ((TONTonTransferItem) value).toJson();
        }
        if (value instanceof TONJettonTransferItem) {
            return ((TONJettonTransferItem) value).toJson();
        }
        if (value instanceof TONNftTransferItem) {
            return ((TONNftTransferItem) value).toJson();
        }
        return value;
    }

    public static TONStructuredItem fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONStructuredItem out = new TONStructuredItem();
        out.type = TonApiJson.optString(json, "type");
        JSONObject payload = json;
        if (TON.equals(out.type)) {
            out.value = TONTonTransferItem.fromJson(payload);
        } else if (JETTON.equals(out.type)) {
            out.value = TONJettonTransferItem.fromJson(payload);
        } else if (NFT.equals(out.type)) {
            out.value = TONNftTransferItem.fromJson(payload);
        } else {
            out.value = payload;
        }
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
