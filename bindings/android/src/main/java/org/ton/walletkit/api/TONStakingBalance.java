//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Staking balance information for a user */
public class TONStakingBalance {

    /** Builds one of these from JSON. */
    public static final Parser<TONStakingBalance> PARSER = new Parser<TONStakingBalance>() {
        @Override
        public TONStakingBalance parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String instantUnstakeAvailable;
    private String providerId;
    private String rawInstantUnstakeAvailable;
    private String rawStakedBalance;
    private String stakedBalance;

    /**
     * Amount available for instant unstake
     * Required by the API.
     */
    public String getInstantUnstakeAvailable() {
        return instantUnstakeAvailable;
    }

    public TONStakingBalance setInstantUnstakeAvailable(String value) {
        this.instantUnstakeAvailable = value;
        return this;
    }

    /**
     * Identifier of the staking provider
     * Required by the API.
     */
    public String getProviderId() {
        return providerId;
    }

    public TONStakingBalance setProviderId(String value) {
        this.providerId = value;
        return this;
    }

    /**
     * Amount available for instant unstake
     * Required by the API.
     */
    public String getRawInstantUnstakeAvailable() {
        return rawInstantUnstakeAvailable;
    }

    public TONStakingBalance setRawInstantUnstakeAvailable(String value) {
        this.rawInstantUnstakeAvailable = value;
        return this;
    }

    /**
     * Amount currently staked
     * Required by the API.
     */
    public String getRawStakedBalance() {
        return rawStakedBalance;
    }

    public TONStakingBalance setRawStakedBalance(String value) {
        this.rawStakedBalance = value;
        return this;
    }

    /**
     * Amount currently staked
     * Required by the API.
     */
    public String getStakedBalance() {
        return stakedBalance;
    }

    public TONStakingBalance setStakedBalance(String value) {
        this.stakedBalance = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "instantUnstakeAvailable", instantUnstakeAvailable);
        TonApiJson.put(json, "providerId", providerId);
        TonApiJson.put(json, "rawInstantUnstakeAvailable", rawInstantUnstakeAvailable);
        TonApiJson.put(json, "rawStakedBalance", rawStakedBalance);
        TonApiJson.put(json, "stakedBalance", stakedBalance);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONStakingBalance fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONStakingBalance out = new TONStakingBalance();
        out.instantUnstakeAvailable = TonApiJson.optString(json, "instantUnstakeAvailable");
        out.providerId = TonApiJson.optString(json, "providerId");
        out.rawInstantUnstakeAvailable = TonApiJson.optString(json, "rawInstantUnstakeAvailable");
        out.rawStakedBalance = TonApiJson.optString(json, "rawStakedBalance");
        out.stakedBalance = TonApiJson.optString(json, "stakedBalance");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
