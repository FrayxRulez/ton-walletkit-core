//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Static metadata for a gasless provider. */
public class TONGaslessProviderMetadata {

    /** Builds one of these from JSON. */
    public static final Parser<TONGaslessProviderMetadata> PARSER = new Parser<TONGaslessProviderMetadata>() {
        @Override
        public TONGaslessProviderMetadata parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String logo;
    private String name;
    private String url;

    public String getLogo() {
        return logo;
    }

    public TONGaslessProviderMetadata setLogo(String value) {
        this.logo = value;
        return this;
    }

    /** Required by the API. */
    public String getName() {
        return name;
    }

    public TONGaslessProviderMetadata setName(String value) {
        this.name = value;
        return this;
    }

    public String getUrl() {
        return url;
    }

    public TONGaslessProviderMetadata setUrl(String value) {
        this.url = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "logo", logo);
        TonApiJson.put(json, "name", name);
        TonApiJson.put(json, "url", url);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONGaslessProviderMetadata fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONGaslessProviderMetadata out = new TONGaslessProviderMetadata();
        out.logo = TonApiJson.optString(json, "logo");
        out.name = TonApiJson.optString(json, "name");
        out.url = TonApiJson.optString(json, "url");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
