//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import java.util.List;

import org.json.JSONArray;
import org.json.JSONObject;

/**
 * Provider-level configuration for a gasless relayer on a given network.
 * Bundles every piece of provider state a consumer needs to drive a gasless transfer end-to-end:  - `relayAddress` — where the relayer wants residual TON (e.g. jetton-transfer    `responseDestination`) returned to.  - `supportedAssets` — what the relayer accepts as fee payment.
 */
public class TONGaslessConfig {

    /** Builds one of these from JSON. */
    public static final Parser<TONGaslessConfig> PARSER = new Parser<TONGaslessConfig>() {
        @Override
        public TONGaslessConfig parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String relayAddress;
    private List<TONGaslessSupportedAsset> supportedAssets;

    /**
     * Relayer address — for jetton-transfer `responseDestination` and similar.
     * Required by the API.
     */
    public String getRelayAddress() {
        return relayAddress;
    }

    public TONGaslessConfig setRelayAddress(String value) {
        this.relayAddress = value;
        return this;
    }

    /**
     * Assets the relayer accepts as fee payment.
     * Required by the API.
     */
    public List<TONGaslessSupportedAsset> getSupportedAssets() {
        return supportedAssets;
    }

    public TONGaslessConfig setSupportedAssets(List<TONGaslessSupportedAsset> value) {
        this.supportedAssets = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "relayAddress", relayAddress);
        if (supportedAssets != null) {
            JSONArray array = new JSONArray();
            for (TONGaslessSupportedAsset item : supportedAssets) {
                array.put(item == null ? JSONObject.NULL : item.toJson());
            }
            TonApiJson.put(json, "supportedAssets", array);
        }
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONGaslessConfig fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONGaslessConfig out = new TONGaslessConfig();
        out.relayAddress = TonApiJson.optString(json, "relayAddress");
        out.supportedAssets = TonApiJson.list(TonApiJson.optArray(json, "supportedAssets"), TONGaslessSupportedAsset.PARSER);
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
