//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Prepared sign data ready for signing by the wallet. */
public class TONPreparedSignData {

    /** Builds one of these from JSON. */
    public static final Parser<TONPreparedSignData> PARSER = new Parser<TONPreparedSignData>() {
        @Override
        public TONPreparedSignData parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String address;
    private String domain;
    private TONSignDataPayload payload;
    private Double timestamp;

    /**
     * Wallet address that will sign the data
     * Required by the API.
     */
    public String getAddress() {
        return address;
    }

    public TONPreparedSignData setAddress(String value) {
        this.address = value;
        return this;
    }

    /**
     * Domain requesting the signature (e.g., "example.com")
     * Required by the API.
     */
    public String getDomain() {
        return domain;
    }

    public TONPreparedSignData setDomain(String value) {
        this.domain = value;
        return this;
    }

    /**
     * Payload containing the data to be signed
     * Required by the API.
     */
    public TONSignDataPayload getPayload() {
        return payload;
    }

    public TONPreparedSignData setPayload(TONSignDataPayload value) {
        this.payload = value;
        return this;
    }

    /**
     * Unix timestamp when the sign request was created
     * Required by the API.
     */
    public Double getTimestamp() {
        return timestamp;
    }

    public TONPreparedSignData setTimestamp(Double value) {
        this.timestamp = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "address", address);
        TonApiJson.put(json, "domain", domain);
        TonApiJson.put(json, "payload", payload == null ? null : payload.toJson());
        TonApiJson.put(json, "timestamp", timestamp);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONPreparedSignData fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONPreparedSignData out = new TONPreparedSignData();
        out.address = TonApiJson.optString(json, "address");
        out.domain = TonApiJson.optString(json, "domain");
        out.payload = TONSignDataPayload.fromJson(TonApiJson.optObject(json, "payload"));
        out.timestamp = TonApiJson.optDouble(json, "timestamp");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
