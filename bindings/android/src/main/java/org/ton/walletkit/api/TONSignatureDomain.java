//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/**
 * A union: {@code type} says which variant this is, and the matching
 * accessor returns it. An unrecognised type leaves the value as the raw
 * JSON rather than failing, so a new variant upstream does not break a
 * response that merely contains one.
 */
public class TONSignatureDomain {

    public static final Parser<TONSignatureDomain> PARSER = new Parser<TONSignatureDomain>() {
        @Override
        public TONSignatureDomain parse(JSONObject json) {
            return fromJson(json);
        }
    };

    public static final String L2 = "l2";
    public static final String EMPTY = "empty";

    private String type;
    private Object value;

    /** Which variant this is; one of the constants above. */
    public String getType() {
        return type;
    }

    /** The variant, typed by {@code type}, or the raw JSON if unrecognised. */
    public Object getValue() {
        return value;
    }

    /** The value when {@code type} is {@code l2}, else null. */
    public TONSignatureDomainL2 asL2() {
        return value instanceof TONSignatureDomainL2 ? (TONSignatureDomainL2) value : null;
    }

    /** The value when {@code type} is {@code empty}, else null. */
    public TONSignatureDomainEmpty asEmpty() {
        return value instanceof TONSignatureDomainEmpty ? (TONSignatureDomainEmpty) value : null;
    }

    public JSONObject toJson() {
        Object encoded = asJson(value);
        JSONObject json = encoded instanceof JSONObject ? (JSONObject) encoded : new JSONObject();
        TonApiJson.put(json, "type", type);
        return json;
    }

    private static Object asJson(Object value) {
        if (value instanceof TONSignatureDomainL2) {
            return ((TONSignatureDomainL2) value).toJson();
        }
        if (value instanceof TONSignatureDomainEmpty) {
            return ((TONSignatureDomainEmpty) value).toJson();
        }
        return value;
    }

    public static TONSignatureDomain fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONSignatureDomain out = new TONSignatureDomain();
        out.type = TonApiJson.optString(json, "type");
        JSONObject payload = json;
        if (L2.equals(out.type)) {
            out.value = TONSignatureDomainL2.fromJson(payload);
        } else if (EMPTY.equals(out.type)) {
            out.value = TONSignatureDomainEmpty.fromJson(payload);
        } else {
            out.value = payload;
        }
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
