//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import java.util.List;

import org.json.JSONObject;

public class TONAddressBookEntry {

    /** Builds one of these from JSON. */
    public static final Parser<TONAddressBookEntry> PARSER = new Parser<TONAddressBookEntry>() {
        @Override
        public TONAddressBookEntry parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String address;
    private String domain;
    private List<String> interfaces;

    /** The human-readable representation of the blockchain address */
    public String getAddress() {
        return address;
    }

    public TONAddressBookEntry setAddress(String value) {
        this.address = value;
        return this;
    }

    /** The domain name associated with the address if available */
    public String getDomain() {
        return domain;
    }

    public TONAddressBookEntry setDomain(String value) {
        this.domain = value;
        return this;
    }

    /**
     * List of supported interfaces by the address
     * Required by the API.
     */
    public List<String> getInterfaces() {
        return interfaces;
    }

    public TONAddressBookEntry setInterfaces(List<String> value) {
        this.interfaces = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "address", address);
        TonApiJson.put(json, "domain", domain);
        TonApiJson.put(json, "interfaces", TonApiJson.array(interfaces));
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONAddressBookEntry fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONAddressBookEntry out = new TONAddressBookEntry();
        out.address = TonApiJson.optString(json, "address");
        out.domain = TonApiJson.optString(json, "domain");
        out.interfaces = TonApiJson.stringList(TonApiJson.optArray(json, "interfaces"));
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
