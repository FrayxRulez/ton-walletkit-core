//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Preview data for displaying transaction request in the wallet UI. */
public class TONSendTransactionRequestEventPreview {

    /** Builds one of these from JSON. */
    public static final Parser<TONSendTransactionRequestEventPreview> PARSER = new Parser<TONSendTransactionRequestEventPreview>() {
        @Override
        public TONSendTransactionRequestEventPreview parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private TONTransactionEmulatedPreview data;

    /** Emulated transaction preview with actions and traces */
    public TONTransactionEmulatedPreview getData() {
        return data;
    }

    public TONSendTransactionRequestEventPreview setData(TONTransactionEmulatedPreview value) {
        this.data = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "data", data == null ? null : data.toJson());
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONSendTransactionRequestEventPreview fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONSendTransactionRequestEventPreview out = new TONSendTransactionRequestEventPreview();
        out.data = TONTransactionEmulatedPreview.fromJson(TonApiJson.optObject(json, "data"));
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
