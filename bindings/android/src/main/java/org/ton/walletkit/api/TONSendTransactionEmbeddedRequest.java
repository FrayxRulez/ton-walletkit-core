//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

public class TONSendTransactionEmbeddedRequest {

    /** Builds one of these from JSON. */
    public static final Parser<TONSendTransactionEmbeddedRequest> PARSER = new Parser<TONSendTransactionEmbeddedRequest>() {
        @Override
        public TONSendTransactionEmbeddedRequest parse(JSONObject json) {
            return fromJson(json);
        }
    };

    /** Values this property accepts. */
    public enum MethodEnum {

        SEND_TRANSACTION("sendTransaction");

        private final String value;

        MethodEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static MethodEnum fromValue(Object wire) {
            if (wire == null) {
                return null;
            }
            String text = wire.toString();
            for (MethodEnum candidate : values()) {
                if (candidate.value.equals(text)) {
                    return candidate;
                }
            }
            return null;
        }
    }

    private MethodEnum method;
    private TONTransactionRequest transactionRequest;

    /** Required by the API. */
    public MethodEnum getMethod() {
        return method;
    }

    public TONSendTransactionEmbeddedRequest setMethod(MethodEnum value) {
        this.method = value;
        return this;
    }

    /** Required by the API. */
    public TONTransactionRequest getTransactionRequest() {
        return transactionRequest;
    }

    public TONSendTransactionEmbeddedRequest setTransactionRequest(TONTransactionRequest value) {
        this.transactionRequest = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "method", method == null ? null : method.getValue());
        TonApiJson.put(json, "transactionRequest", transactionRequest == null ? null : transactionRequest.toJson());
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONSendTransactionEmbeddedRequest fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONSendTransactionEmbeddedRequest out = new TONSendTransactionEmbeddedRequest();
        out.method = MethodEnum.fromValue(TonApiJson.opt(json, "method"));
        out.transactionRequest = TONTransactionRequest.fromJson(TonApiJson.optObject(json, "transactionRequest"));
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
