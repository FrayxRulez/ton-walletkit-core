//
// TonWalletAdapter — part of the wallet object model.
//
// GENERATED by scripts/generate-facade.mjs from api/facade.json — do not edit.
// Change the schema, then regenerate.
//
package org.ton.walletkit.core;

import java.util.List;

import org.ton.walletkit.api.*;

/**
 * A wallet contract bound to a signer (kit-ios TONWalletAdapterProtocol).
 * It knows its address before it is registered with the kit, and it can sign —
 * but it cannot read the chain until it becomes a Wallet.
 */
public interface TonWalletAdapter {

    /** The wallet id this adapter derives (kit-ios identifier()). */
    String getIdentifier();

    /** Public key, 0x-prefixed hex. */
    String getPublicKey();

    /** The network this wallet lives on. */
    TONNetwork getNetwork();

    /** User-friendly address (UQ…/EQ…). */
    String getAddress();

    /** Deployment state init, a base64 BOC. */
    void stateInit(Callback<String> callback);

    /**
     * Signs a transaction and returns the BOC.
     * fakeSignature produces a correctly shaped but unusable signature — right
     * for fee estimation, and what tests use so nothing can be broadcast by
     * accident.
     */
    void signedSendTransaction(TONTransactionRequest input, TONSignedSendTransactionOptions options, Callback<String> callback);

    /** Signs a transaction as a TON Connect signMessage response. */
    void signedSignMessage(TONTransactionRequest input, TONSignedSendTransactionOptions options, Callback<String> callback);

    /** Signs prepared sign-data; returns a hex signature. */
    void signedSignData(TONPreparedSignData input, Boolean fakeSignature, Callback<String> callback);

    /** Signs a ton_proof message; returns a hex signature. */
    void signedTonProof(TONProofMessage input, Boolean fakeSignature, Callback<String> callback);

    /** TON Connect features this wallet advertises. */
    void supportedFeatures(Callback<List<TonFeature>> callback);

}
