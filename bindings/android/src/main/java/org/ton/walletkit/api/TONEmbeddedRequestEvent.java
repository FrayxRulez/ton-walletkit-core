//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/**
 * A union: {@code type} says which variant this is, and the matching
 * accessor returns it. An unrecognised type leaves the value as the raw
 * JSON rather than failing, so a new variant upstream does not break a
 * response that merely contains one.
 */
public class TONEmbeddedRequestEvent {

    public static final Parser<TONEmbeddedRequestEvent> PARSER = new Parser<TONEmbeddedRequestEvent>() {
        @Override
        public TONEmbeddedRequestEvent parse(JSONObject json) {
            return fromJson(json);
        }
    };

    public static final String SEND_TRANSACTION = "sendTransaction";
    public static final String SIGN_MESSAGE = "signMessage";
    public static final String SIGN_DATA = "signData";

    private String type;
    private Object value;

    /** Which variant this is; one of the constants above. */
    public String getType() {
        return type;
    }

    /** The variant, typed by {@code type}, or the raw JSON if unrecognised. */
    public Object getValue() {
        return value;
    }

    /** The value when {@code type} is {@code sendTransaction}, else null. */
    public TONEmbeddedSendTransactionRequestEvent asSendTransaction() {
        return value instanceof TONEmbeddedSendTransactionRequestEvent ? (TONEmbeddedSendTransactionRequestEvent) value : null;
    }

    /** The value when {@code type} is {@code signMessage}, else null. */
    public TONEmbeddedSignMessageRequestEvent asSignMessage() {
        return value instanceof TONEmbeddedSignMessageRequestEvent ? (TONEmbeddedSignMessageRequestEvent) value : null;
    }

    /** The value when {@code type} is {@code signData}, else null. */
    public TONEmbeddedSignDataRequestEvent asSignData() {
        return value instanceof TONEmbeddedSignDataRequestEvent ? (TONEmbeddedSignDataRequestEvent) value : null;
    }

    public JSONObject toJson() {
        Object encoded = asJson(value);
        JSONObject json = encoded instanceof JSONObject ? (JSONObject) encoded : new JSONObject();
        TonApiJson.put(json, "type", type);
        return json;
    }

    private static Object asJson(Object value) {
        if (value instanceof TONEmbeddedSendTransactionRequestEvent) {
            return ((TONEmbeddedSendTransactionRequestEvent) value).toJson();
        }
        if (value instanceof TONEmbeddedSignMessageRequestEvent) {
            return ((TONEmbeddedSignMessageRequestEvent) value).toJson();
        }
        if (value instanceof TONEmbeddedSignDataRequestEvent) {
            return ((TONEmbeddedSignDataRequestEvent) value).toJson();
        }
        return value;
    }

    public static TONEmbeddedRequestEvent fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONEmbeddedRequestEvent out = new TONEmbeddedRequestEvent();
        out.type = TonApiJson.optString(json, "type");
        JSONObject payload = json;
        if (SEND_TRANSACTION.equals(out.type)) {
            out.value = TONEmbeddedSendTransactionRequestEvent.fromJson(payload);
        } else if (SIGN_MESSAGE.equals(out.type)) {
            out.value = TONEmbeddedSignMessageRequestEvent.fromJson(payload);
        } else if (SIGN_DATA.equals(out.type)) {
            out.value = TONEmbeddedSignDataRequestEvent.fromJson(payload);
        } else {
            out.value = payload;
        }
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
