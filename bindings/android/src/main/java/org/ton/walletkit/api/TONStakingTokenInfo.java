//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

public class TONStakingTokenInfo {

    /** Builds one of these from JSON. */
    public static final Parser<TONStakingTokenInfo> PARSER = new Parser<TONStakingTokenInfo>() {
        @Override
        public TONStakingTokenInfo parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String address;
    private Double decimals;
    private String ticker;

    /**
     * 'ton' for native TON, otherwise contract address in friendly format
     * Required by the API.
     */
    public String getAddress() {
        return address;
    }

    public TONStakingTokenInfo setAddress(String value) {
        this.address = value;
        return this;
    }

    /** Required by the API. */
    public Double getDecimals() {
        return decimals;
    }

    public TONStakingTokenInfo setDecimals(Double value) {
        this.decimals = value;
        return this;
    }

    /** Required by the API. */
    public String getTicker() {
        return ticker;
    }

    public TONStakingTokenInfo setTicker(String value) {
        this.ticker = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "address", address);
        TonApiJson.put(json, "decimals", decimals);
        TonApiJson.put(json, "ticker", ticker);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONStakingTokenInfo fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONStakingTokenInfo out = new TONStakingTokenInfo();
        out.address = TonApiJson.optString(json, "address");
        out.decimals = TonApiJson.optDouble(json, "decimals");
        out.ticker = TonApiJson.optString(json, "ticker");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
