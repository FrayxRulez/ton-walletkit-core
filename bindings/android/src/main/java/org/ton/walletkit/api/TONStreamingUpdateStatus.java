//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

/** Represents the status of a streaming notification. */
public enum TONStreamingUpdateStatus {

    PENDING("pending"),
    CONFIRMED("confirmed"),
    FINALIZED("finalized"),
    INVALIDATED("invalidated");

    /** Reads one of these from a wire value; for arrays of this enum. */
    public static final EnumParser<TONStreamingUpdateStatus> VALUES = new EnumParser<TONStreamingUpdateStatus>() {
        @Override
        public TONStreamingUpdateStatus parse(Object wire) {
            return fromValue(wire);
        }
    };

    private final String value;

    TONStreamingUpdateStatus(String value) {
        this.value = value;
    }

    public String getValue() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    /** The constant for a wire value, or null if it is absent or unknown. */
    public static TONStreamingUpdateStatus fromValue(Object wire) {
        if (wire == null) {
            return null;
        }
        String text = wire.toString();
        for (TONStreamingUpdateStatus candidate : values()) {
            if (candidate.value.equals(text)) {
                return candidate;
            }
        }
        return null;
    }
}
