//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Preview data for displaying disconnection event in the wallet UI. */
public class TONDisconnectionEventPreview {

    /** Builds one of these from JSON. */
    public static final Parser<TONDisconnectionEventPreview> PARSER = new Parser<TONDisconnectionEventPreview>() {
        @Override
        public TONDisconnectionEventPreview parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private TONDAppInfo dAppInfo;
    private String reason;

    /** Information about the disconnected dApp */
    public TONDAppInfo getDAppInfo() {
        return dAppInfo;
    }

    public TONDisconnectionEventPreview setDAppInfo(TONDAppInfo value) {
        this.dAppInfo = value;
        return this;
    }

    /** Human-readable reason for the disconnection */
    public String getReason() {
        return reason;
    }

    public TONDisconnectionEventPreview setReason(String value) {
        this.reason = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "dAppInfo", dAppInfo == null ? null : dAppInfo.toJson());
        TonApiJson.put(json, "reason", reason);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONDisconnectionEventPreview fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONDisconnectionEventPreview out = new TONDisconnectionEventPreview();
        out.dAppInfo = TONDAppInfo.fromJson(TonApiJson.optObject(json, "dAppInfo"));
        out.reason = TonApiJson.optString(json, "reason");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
