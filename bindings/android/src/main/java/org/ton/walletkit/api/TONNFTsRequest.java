//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Request parameters for fetching NFT tokens. */
public class TONNFTsRequest {

    /** Builds one of these from JSON. */
    public static final Parser<TONNFTsRequest> PARSER = new Parser<TONNFTsRequest>() {
        @Override
        public TONNFTsRequest parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String address;
    private TONPagination pagination;

    /** NFT address to filter by */
    public String getAddress() {
        return address;
    }

    public TONNFTsRequest setAddress(String value) {
        this.address = value;
        return this;
    }

    /** Pagination information */
    public TONPagination getPagination() {
        return pagination;
    }

    public TONNFTsRequest setPagination(TONPagination value) {
        this.pagination = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "address", address);
        TonApiJson.put(json, "pagination", pagination == null ? null : pagination.toJson());
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONNFTsRequest fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONNFTsRequest out = new TONNFTsRequest();
        out.address = TonApiJson.optString(json, "address");
        out.pagination = TONPagination.fromJson(TonApiJson.optObject(json, "pagination"));
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
