//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import java.util.List;

import org.json.JSONArray;
import org.json.JSONObject;

public class TONTransactionsUpdate {

    /** Builds one of these from JSON. */
    public static final Parser<TONTransactionsUpdate> PARSER = new Parser<TONTransactionsUpdate>() {
        @Override
        public TONTransactionsUpdate parse(JSONObject json) {
            return fromJson(json);
        }
    };

    /** Values this property accepts. */
    public enum TypeEnum {

        TRANSACTIONS("transactions");

        private final String value;

        TypeEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static TypeEnum fromValue(Object wire) {
            if (wire == null) {
                return null;
            }
            String text = wire.toString();
            for (TypeEnum candidate : values()) {
                if (candidate.value.equals(text)) {
                    return candidate;
                }
            }
            return null;
        }
    }

    private String address;
    private TONAddressBook addressBook;
    private TONTransactionAddressMetadata metadata;
    private TONStreamingUpdateStatus status;
    private List<TONTransaction> transactions;
    private TypeEnum type;

    /**
     * The account address
     * Required by the API.
     */
    public String getAddress() {
        return address;
    }

    public TONTransactionsUpdate setAddress(String value) {
        this.address = value;
        return this;
    }

    /** Address book from streaming v2 notification */
    public TONAddressBook getAddressBook() {
        return addressBook;
    }

    public TONTransactionsUpdate setAddressBook(TONAddressBook value) {
        this.addressBook = value;
        return this;
    }

    /** Metadata from streaming v2 notification */
    public TONTransactionAddressMetadata getMetadata() {
        return metadata;
    }

    public TONTransactionsUpdate setMetadata(TONTransactionAddressMetadata value) {
        this.metadata = value;
        return this;
    }

    /**
     * The finality of the update
     * Required by the API.
     */
    public TONStreamingUpdateStatus getStatus() {
        return status;
    }

    public TONTransactionsUpdate setStatus(TONStreamingUpdateStatus value) {
        this.status = value;
        return this;
    }

    /**
     * The array of transactions
     * Required by the API.
     */
    public List<TONTransaction> getTransactions() {
        return transactions;
    }

    public TONTransactionsUpdate setTransactions(List<TONTransaction> value) {
        this.transactions = value;
        return this;
    }

    /**
     * The update type field
     * Required by the API.
     */
    public TypeEnum getType() {
        return type;
    }

    public TONTransactionsUpdate setType(TypeEnum value) {
        this.type = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "address", address);
        TonApiJson.put(json, "addressBook", addressBook == null ? null : addressBook.toJson());
        TonApiJson.put(json, "metadata", metadata == null ? null : metadata.toJson());
        TonApiJson.put(json, "status", status == null ? null : status.getValue());
        if (transactions != null) {
            JSONArray array = new JSONArray();
            for (TONTransaction item : transactions) {
                array.put(item == null ? JSONObject.NULL : item.toJson());
            }
            TonApiJson.put(json, "transactions", array);
        }
        TonApiJson.put(json, "type", type == null ? null : type.getValue());
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONTransactionsUpdate fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONTransactionsUpdate out = new TONTransactionsUpdate();
        out.address = TonApiJson.optString(json, "address");
        out.addressBook = TONAddressBook.fromJson(TonApiJson.optObject(json, "addressBook"));
        out.metadata = TONTransactionAddressMetadata.fromJson(TonApiJson.optObject(json, "metadata"));
        out.status = TONStreamingUpdateStatus.fromValue(TonApiJson.opt(json, "status"));
        out.transactions = TonApiJson.list(TonApiJson.optArray(json, "transactions"), TONTransaction.PARSER);
        out.type = TypeEnum.fromValue(TonApiJson.opt(json, "type"));
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
