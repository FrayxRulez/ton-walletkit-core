//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

public enum TONSendModeBase {

    _0(0d),
    _64(64d),
    _128(128d);

    /** Reads one of these from a wire value; for arrays of this enum. */
    public static final EnumParser<TONSendModeBase> VALUES = new EnumParser<TONSendModeBase>() {
        @Override
        public TONSendModeBase parse(Object wire) {
            return fromValue(wire);
        }
    };

    private final Double value;

    TONSendModeBase(Double value) {
        this.value = value;
    }

    public Double getValue() {
        return value;
    }

    @Override
    public String toString() {
        return String.valueOf(value);
    }

    /** The constant for a wire value, or null if it is absent or unknown. */
    public static TONSendModeBase fromValue(Object wire) {
        if (wire == null) {
            return null;
        }
        double number = wire instanceof Number ? ((Number) wire).doubleValue()
                : Double.parseDouble(wire.toString());
        for (TONSendModeBase candidate : values()) {
            if (candidate.value.doubleValue() == number) {
                return candidate;
            }
        }
        return null;
    }
}
