/*
 * TON WalletKit models
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.ton.walletkit.api;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import org.ton.walletkit.api.TONTransactionTokenInfo;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import org.ton.walletkit.api.JSON;

/**
 * Metadata entry for a specific address in a transaction.
 */
public class TONTransactionAddressMetadataEntry {
  public static final String SERIALIZED_NAME_TOKEN_INFO = "tokenInfo";
  @SerializedName(SERIALIZED_NAME_TOKEN_INFO)
  private List<TONTransactionTokenInfo> tokenInfo = new ArrayList<>();

  public TONTransactionAddressMetadataEntry() {
  }

  public TONTransactionAddressMetadataEntry tokenInfo(List<TONTransactionTokenInfo> tokenInfo) {
    this.tokenInfo = tokenInfo;
    return this;
  }

  public TONTransactionAddressMetadataEntry addTokenInfoItem(TONTransactionTokenInfo tokenInfoItem) {
    if (this.tokenInfo == null) {
      this.tokenInfo = new ArrayList<>();
    }
    this.tokenInfo.add(tokenInfoItem);
    return this;
  }

  /**
   * Token information associated with this address, if any
   * @return tokenInfo
   */
  public List<TONTransactionTokenInfo> getTokenInfo() {
    return tokenInfo;
  }

  public void setTokenInfo(List<TONTransactionTokenInfo> tokenInfo) {
    this.tokenInfo = tokenInfo;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    TONTransactionAddressMetadataEntry transactionAddressMetadataEntry = (TONTransactionAddressMetadataEntry) o;
    return Objects.equals(this.tokenInfo, transactionAddressMetadataEntry.tokenInfo);
  }

  @Override
  public int hashCode() {
    return Objects.hash(tokenInfo);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class TONTransactionAddressMetadataEntry {\n");
    sb.append("    tokenInfo: ").append(toIndentedString(tokenInfo)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("tokenInfo");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to TONTransactionAddressMetadataEntry
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!TONTransactionAddressMetadataEntry.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in TONTransactionAddressMetadataEntry is not found in the empty JSON string", TONTransactionAddressMetadataEntry.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!TONTransactionAddressMetadataEntry.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `TONTransactionAddressMetadataEntry` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("tokenInfo") != null && !jsonObj.get("tokenInfo").isJsonNull()) {
        JsonArray jsonArraytokenInfo = jsonObj.getAsJsonArray("tokenInfo");
        if (jsonArraytokenInfo != null) {
          // ensure the json data is an array
          if (!jsonObj.get("tokenInfo").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `tokenInfo` to be an array in the JSON string but got `%s`", jsonObj.get("tokenInfo").toString()));
          }

          // validate the optional field `tokenInfo` (array)
          for (int i = 0; i < jsonArraytokenInfo.size(); i++) {
            TONTransactionTokenInfo.validateJsonElement(jsonArraytokenInfo.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!TONTransactionAddressMetadataEntry.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'TONTransactionAddressMetadataEntry' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<TONTransactionAddressMetadataEntry> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(TONTransactionAddressMetadataEntry.class));

       return (TypeAdapter<T>) new TypeAdapter<TONTransactionAddressMetadataEntry>() {
           @Override
           public void write(JsonWriter out, TONTransactionAddressMetadataEntry value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public TONTransactionAddressMetadataEntry read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of TONTransactionAddressMetadataEntry given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of TONTransactionAddressMetadataEntry
   * @throws IOException if the JSON string is invalid with respect to TONTransactionAddressMetadataEntry
   */
  public static TONTransactionAddressMetadataEntry fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, TONTransactionAddressMetadataEntry.class);
  }

  /**
   * Convert an instance of TONTransactionAddressMetadataEntry to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

