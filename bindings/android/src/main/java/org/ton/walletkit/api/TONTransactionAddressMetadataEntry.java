//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import java.util.List;

import org.json.JSONArray;
import org.json.JSONObject;

/** Metadata entry for a specific address in a transaction. */
public class TONTransactionAddressMetadataEntry {

    /** Builds one of these from JSON. */
    public static final Parser<TONTransactionAddressMetadataEntry> PARSER = new Parser<TONTransactionAddressMetadataEntry>() {
        @Override
        public TONTransactionAddressMetadataEntry parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private List<TONTransactionTokenInfo> tokenInfo;

    /** Token information associated with this address, if any */
    public List<TONTransactionTokenInfo> getTokenInfo() {
        return tokenInfo;
    }

    public TONTransactionAddressMetadataEntry setTokenInfo(List<TONTransactionTokenInfo> value) {
        this.tokenInfo = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        if (tokenInfo != null) {
            JSONArray array = new JSONArray();
            for (TONTransactionTokenInfo item : tokenInfo) {
                array.put(item == null ? JSONObject.NULL : item.toJson());
            }
            TonApiJson.put(json, "tokenInfo", array);
        }
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONTransactionAddressMetadataEntry fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONTransactionAddressMetadataEntry out = new TONTransactionAddressMetadataEntry();
        out.tokenInfo = TonApiJson.list(TonApiJson.optArray(json, "tokenInfo"), TONTransactionTokenInfo.PARSER);
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
