//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Preview data for displaying sign data request in the wallet UI. */
public class TONSignDataRequestEventPreview {

    /** Builds one of these from JSON. */
    public static final Parser<TONSignDataRequestEventPreview> PARSER = new Parser<TONSignDataRequestEventPreview>() {
        @Override
        public TONSignDataRequestEventPreview parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private TONDAppInfo dAppInfo;
    private TONSignDataPreview data;

    /** Information about the requesting dApp */
    public TONDAppInfo getDAppInfo() {
        return dAppInfo;
    }

    public TONSignDataRequestEventPreview setDAppInfo(TONDAppInfo value) {
        this.dAppInfo = value;
        return this;
    }

    /**
     * Array of sign data previews
     * Required by the API.
     */
    public TONSignDataPreview getData() {
        return data;
    }

    public TONSignDataRequestEventPreview setData(TONSignDataPreview value) {
        this.data = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "dAppInfo", dAppInfo == null ? null : dAppInfo.toJson());
        TonApiJson.put(json, "data", data == null ? null : data.toJson());
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONSignDataRequestEventPreview fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONSignDataRequestEventPreview out = new TONSignDataRequestEventPreview();
        out.dAppInfo = TONDAppInfo.fromJson(TonApiJson.optObject(json, "dAppInfo"));
        out.data = TONSignDataPreview.fromJson(TonApiJson.optObject(json, "data"));
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
