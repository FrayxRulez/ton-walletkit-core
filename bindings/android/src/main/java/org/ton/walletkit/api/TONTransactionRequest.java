//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import java.util.List;

import org.json.JSONArray;
import org.json.JSONObject;

/** Request to send a transaction on the TON blockchain. Contains `messages` or `items`. If items are present, but messages are not — wallet app is responsible for resolving items into messages. */
public class TONTransactionRequest {

    /** Builds one of these from JSON. */
    public static final Parser<TONTransactionRequest> PARSER = new Parser<TONTransactionRequest>() {
        @Override
        public TONTransactionRequest parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String fromAddress;
    private List<TONStructuredItem> items;
    private List<TONTransactionRequestMessage> messages;
    private TONNetwork network;
    private Double validUntil;

    /** Sender wallet address in received format(raw, user friendly) */
    public String getFromAddress() {
        return fromAddress;
    }

    public TONTransactionRequest setFromAddress(String value) {
        this.fromAddress = value;
        return this;
    }

    /** List of structured items (ton/jetton/nft) as an alternative to raw messages. When present, the wallet app is responsible for resolving items into messages. */
    public List<TONStructuredItem> getItems() {
        return items;
    }

    public TONTransactionRequest setItems(List<TONStructuredItem> value) {
        this.items = value;
        return this;
    }

    /**
     * List of messages to include in the transaction
     * Required by the API.
     */
    public List<TONTransactionRequestMessage> getMessages() {
        return messages;
    }

    public TONTransactionRequest setMessages(List<TONTransactionRequestMessage> value) {
        this.messages = value;
        return this;
    }

    /** Network to execute the transaction on */
    public TONNetwork getNetwork() {
        return network;
    }

    public TONTransactionRequest setNetwork(TONNetwork value) {
        this.network = value;
        return this;
    }

    /** Unix timestamp after which the transaction becomes invalid */
    public Double getValidUntil() {
        return validUntil;
    }

    public TONTransactionRequest setValidUntil(Double value) {
        this.validUntil = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "fromAddress", fromAddress);
        if (items != null) {
            JSONArray array = new JSONArray();
            for (TONStructuredItem item : items) {
                array.put(item == null ? JSONObject.NULL : item.toJson());
            }
            TonApiJson.put(json, "items", array);
        }
        if (messages != null) {
            JSONArray array = new JSONArray();
            for (TONTransactionRequestMessage item : messages) {
                array.put(item == null ? JSONObject.NULL : item.toJson());
            }
            TonApiJson.put(json, "messages", array);
        }
        TonApiJson.put(json, "network", network == null ? null : network.toJson());
        TonApiJson.put(json, "validUntil", validUntil);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONTransactionRequest fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONTransactionRequest out = new TONTransactionRequest();
        out.fromAddress = TonApiJson.optString(json, "fromAddress");
        out.items = TonApiJson.list(TonApiJson.optArray(json, "items"), TONStructuredItem.PARSER);
        out.messages = TonApiJson.list(TonApiJson.optArray(json, "messages"), TONTransactionRequestMessage.PARSER);
        out.network = TONNetwork.fromJson(TonApiJson.optObject(json, "network"));
        out.validUntil = TonApiJson.optDouble(json, "validUntil");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
