//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

public class TONTonTransferItem {

    /** Builds one of these from JSON. */
    public static final Parser<TONTonTransferItem> PARSER = new Parser<TONTonTransferItem>() {
        @Override
        public TONTonTransferItem parse(JSONObject json) {
            return fromJson(json);
        }
    };

    /** Values this property accepts. */
    public enum TypeEnum {

        TON("ton");

        private final String value;

        TypeEnum(String value) {
            this.value = value;
        }

        public String getValue() {
            return value;
        }

        @Override
        public String toString() {
            return String.valueOf(value);
        }

        public static TypeEnum fromValue(Object wire) {
            if (wire == null) {
                return null;
            }
            String text = wire.toString();
            for (TypeEnum candidate : values()) {
                if (candidate.value.equals(text)) {
                    return candidate;
                }
            }
            return null;
        }
    }

    private String address;
    private String amount;
    private TONExtraCurrencies extraCurrency;
    private String payload;
    private String stateInit;
    private TypeEnum type;

    /** Required by the API. */
    public String getAddress() {
        return address;
    }

    public TONTonTransferItem setAddress(String value) {
        this.address = value;
        return this;
    }

    /** Required by the API. */
    public String getAmount() {
        return amount;
    }

    public TONTonTransferItem setAmount(String value) {
        this.amount = value;
        return this;
    }

    public TONExtraCurrencies getExtraCurrency() {
        return extraCurrency;
    }

    public TONTonTransferItem setExtraCurrency(TONExtraCurrencies value) {
        this.extraCurrency = value;
        return this;
    }

    public String getPayload() {
        return payload;
    }

    public TONTonTransferItem setPayload(String value) {
        this.payload = value;
        return this;
    }

    public String getStateInit() {
        return stateInit;
    }

    public TONTonTransferItem setStateInit(String value) {
        this.stateInit = value;
        return this;
    }

    /** Required by the API. */
    public TypeEnum getType() {
        return type;
    }

    public TONTonTransferItem setType(TypeEnum value) {
        this.type = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "address", address);
        TonApiJson.put(json, "amount", amount);
        TonApiJson.put(json, "extraCurrency", extraCurrency == null ? null : extraCurrency.toJson());
        TonApiJson.put(json, "payload", payload);
        TonApiJson.put(json, "stateInit", stateInit);
        TonApiJson.put(json, "type", type == null ? null : type.getValue());
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONTonTransferItem fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONTonTransferItem out = new TONTonTransferItem();
        out.address = TonApiJson.optString(json, "address");
        out.amount = TonApiJson.optString(json, "amount");
        out.extraCurrency = TONExtraCurrencies.fromJson(TonApiJson.optObject(json, "extraCurrency"));
        out.payload = TonApiJson.optString(json, "payload");
        out.stateInit = TonApiJson.optString(json, "stateInit");
        out.type = TypeEnum.fromValue(TonApiJson.opt(json, "type"));
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
