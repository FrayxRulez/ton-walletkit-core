//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Details of a transaction trace action */
/**
 * A union: {@code type} says which variant this is, and the matching
 * accessor returns it. An unrecognised type leaves the value as the raw
 * JSON rather than failing, so a new variant upstream does not break a
 * response that merely contains one.
 */
public class TONTransactionTraceActionDetails {

    public static final Parser<TONTransactionTraceActionDetails> PARSER = new Parser<TONTransactionTraceActionDetails>() {
        @Override
        public TONTransactionTraceActionDetails parse(JSONObject json) {
            return fromJson(json);
        }
    };

    public static final String JETTON_SWAP = "jetton_swap";
    public static final String CALL_CONTRACT = "call_contract";
    public static final String TON_TRANSFER = "ton_transfer";
    public static final String UNKNOWN = "unknown";

    private String type;
    private Object value;

    /** Which variant this is; one of the constants above. */
    public String getType() {
        return type;
    }

    /** The variant, typed by {@code type}, or the raw JSON if unrecognised. */
    public Object getValue() {
        return value;
    }

    /** The value when {@code type} is {@code jetton_swap}, else null. */
    public TONTransactionTraceActionJettonSwapDetails asJettonSwap() {
        return value instanceof TONTransactionTraceActionJettonSwapDetails ? (TONTransactionTraceActionJettonSwapDetails) value : null;
    }

    /** The value when {@code type} is {@code call_contract}, else null. */
    public TONTransactionTraceActionCallContractDetails asCallContract() {
        return value instanceof TONTransactionTraceActionCallContractDetails ? (TONTransactionTraceActionCallContractDetails) value : null;
    }

    /** The value when {@code type} is {@code ton_transfer}, else null. */
    public TONTransactionTraceActionTONTransferDetails asTonTransfer() {
        return value instanceof TONTransactionTraceActionTONTransferDetails ? (TONTransactionTraceActionTONTransferDetails) value : null;
    }

    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "type", type);
        TonApiJson.put(json, "value", asJson(value));
        return json;
    }

    private static Object asJson(Object value) {
        if (value instanceof TONTransactionTraceActionJettonSwapDetails) {
            return ((TONTransactionTraceActionJettonSwapDetails) value).toJson();
        }
        if (value instanceof TONTransactionTraceActionCallContractDetails) {
            return ((TONTransactionTraceActionCallContractDetails) value).toJson();
        }
        if (value instanceof TONTransactionTraceActionTONTransferDetails) {
            return ((TONTransactionTraceActionTONTransferDetails) value).toJson();
        }
        return value;
    }

    public static TONTransactionTraceActionDetails fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONTransactionTraceActionDetails out = new TONTransactionTraceActionDetails();
        out.type = TonApiJson.optString(json, "type");
        JSONObject payload = TonApiJson.optObject(json, "value");
        if (JETTON_SWAP.equals(out.type)) {
            out.value = TONTransactionTraceActionJettonSwapDetails.fromJson(payload);
        } else if (CALL_CONTRACT.equals(out.type)) {
            out.value = TONTransactionTraceActionCallContractDetails.fromJson(payload);
        } else if (TON_TRANSFER.equals(out.type)) {
            out.value = TONTransactionTraceActionTONTransferDetails.fromJson(payload);
        } else if (UNKNOWN.equals(out.type)) {
            out.value = payload;
        } else {
            out.value = payload;
        }
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
