//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Data to be signed by the wallet, discriminated by type. */
/**
 * A union: {@code type} says which variant this is, and the matching
 * accessor returns it. An unrecognised type leaves the value as the raw
 * JSON rather than failing, so a new variant upstream does not break a
 * response that merely contains one.
 */
public class TONConnectionRequestEventRequestedItem {

    public static final Parser<TONConnectionRequestEventRequestedItem> PARSER = new Parser<TONConnectionRequestEventRequestedItem>() {
        @Override
        public TONConnectionRequestEventRequestedItem parse(JSONObject json) {
            return fromJson(json);
        }
    };

    public static final String TON_ADDR = "ton_addr";
    public static final String TON_PROOF = "ton_proof";
    public static final String UNKNOWN = "unknown";

    private String type;
    private Object value;

    /** Which variant this is; one of the constants above. */
    public String getType() {
        return type;
    }

    /** The variant, typed by {@code type}, or the raw JSON if unrecognised. */
    public Object getValue() {
        return value;
    }

    /** The value when {@code type} is {@code ton_proof}, else null. */
    public TONConnectionRequestTonProofRequestedItem asTonProof() {
        return value instanceof TONConnectionRequestTonProofRequestedItem ? (TONConnectionRequestTonProofRequestedItem) value : null;
    }

    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "type", type);
        TonApiJson.put(json, "value", asJson(value));
        return json;
    }

    private static Object asJson(Object value) {
        if (value instanceof TONConnectionRequestTonProofRequestedItem) {
            return ((TONConnectionRequestTonProofRequestedItem) value).toJson();
        }
        return value;
    }

    public static TONConnectionRequestEventRequestedItem fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONConnectionRequestEventRequestedItem out = new TONConnectionRequestEventRequestedItem();
        out.type = TonApiJson.optString(json, "type");
        JSONObject payload = TonApiJson.optObject(json, "value");
        if (TON_ADDR.equals(out.type)) {
            out.value = payload;
        } else if (TON_PROOF.equals(out.type)) {
            out.value = TONConnectionRequestTonProofRequestedItem.fromJson(payload);
        } else if (UNKNOWN.equals(out.type)) {
            out.value = payload;
        } else {
            out.value = payload;
        }
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
