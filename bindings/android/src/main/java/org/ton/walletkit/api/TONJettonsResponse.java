//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import java.util.List;

import org.json.JSONArray;
import org.json.JSONObject;

/** Response containing a list of Jetton tokens. */
public class TONJettonsResponse {

    /** Builds one of these from JSON. */
    public static final Parser<TONJettonsResponse> PARSER = new Parser<TONJettonsResponse>() {
        @Override
        public TONJettonsResponse parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private TONAddressBook addressBook;
    private List<TONJetton> jettons;

    /**
     * Address book mapping
     * Required by the API.
     */
    public TONAddressBook getAddressBook() {
        return addressBook;
    }

    public TONJettonsResponse setAddressBook(TONAddressBook value) {
        this.addressBook = value;
        return this;
    }

    /**
     * List of Jettons
     * Required by the API.
     */
    public List<TONJetton> getJettons() {
        return jettons;
    }

    public TONJettonsResponse setJettons(List<TONJetton> value) {
        this.jettons = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "addressBook", addressBook == null ? null : addressBook.toJson());
        if (jettons != null) {
            JSONArray array = new JSONArray();
            for (TONJetton item : jettons) {
                array.put(item == null ? JSONObject.NULL : item.toJson());
            }
            TonApiJson.put(json, "jettons", array);
        }
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONJettonsResponse fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONJettonsResponse out = new TONJettonsResponse();
        out.addressBook = TONAddressBook.fromJson(TonApiJson.optObject(json, "addressBook"));
        out.jettons = TonApiJson.list(TonApiJson.optArray(json, "jettons"), TONJetton.PARSER);
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
