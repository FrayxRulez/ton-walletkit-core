//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Error information returned when an operation fails. */
public class TONResultError {

    /** Builds one of these from JSON. */
    public static final Parser<TONResultError> PARSER = new Parser<TONResultError>() {
        @Override
        public TONResultError parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private Double code;
    private Object data;
    private String message;

    /** Error code representing the type of error */
    public Double getCode() {
        return code;
    }

    public TONResultError setCode(Double value) {
        this.code = value;
        return this;
    }

    /** Additional error data */
    public Object getData() {
        return data;
    }

    public TONResultError setData(Object value) {
        this.data = value;
        return this;
    }

    /** Human-readable error message */
    public String getMessage() {
        return message;
    }

    public TONResultError setMessage(String value) {
        this.message = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "code", code);
        TonApiJson.put(json, "data", data);
        TonApiJson.put(json, "message", message);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONResultError fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONResultError out = new TONResultError();
        out.code = TonApiJson.optDouble(json, "code");
        out.data = TonApiJson.opt(json, "data");
        out.message = TonApiJson.optString(json, "message");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
