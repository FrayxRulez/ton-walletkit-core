//
// TonWallet — part of the wallet object model.
//
// GENERATED by scripts/generate-facade.mjs from api/facade.json — do not edit.
// Change the schema, then regenerate.
//
package org.ton.walletkit.core;

import java.util.List;

import org.ton.walletkit.api.*;

/**
 * A wallet registered with the kit (kit-ios TONWalletProtocol).
 *
 * This is a handle, not storage: walletkit keeps wallets in memory only, so
 * after a restart the app rebuilds them from the persisted mnemonic.
 */
public interface TonWallet extends TonWalletAdapter {

    /** The kit's wallet id (a base64 hash, not network:address). */
    String getId();

    /** Current balance, in nanotons. */
    void balance(Callback<TonAmount> callback);

    /**
     * Builds an unsigned TON transfer. The recipient must be in user-friendly
     * form (UQ…/EQ…): a raw address passes the early check and is then rejected
     * with a message that names the transaction, not the address.
     */
    void transferTonTransaction(TONTransferRequest request, Callback<TONTransactionRequest> callback);

    /** Builds one transaction carrying several transfers. */
    void transferTonTransaction(List<TONTransferRequest> requests, Callback<TONTransactionRequest> callback);

    /** Signs and broadcasts. This spends real funds. */
    void send(TONTransactionRequest transactionRequest, Callback<TONSendTransactionResponse> callback);

    /** Emulates a transaction: fees, balance changes, actions. */
    void preview(TONTransactionRequest transactionRequest, TONTransactionPreviewOptions options, Callback<TONTransactionEmulatedPreview> callback);

    /** Builds an NFT transfer. */
    void transferNftTransaction(TONNFTTransferRequest request, Callback<TONTransactionRequest> callback);

    /** Builds an NFT transfer from a raw message. */
    void transferNftTransaction(TONNFTRawTransferRequest request, Callback<TONTransactionRequest> callback);

    /** NFTs this wallet holds. */
    void nfts(TONNFTsRequest request, Callback<TONNFTsResponse> callback);

    /** One NFT by address, or null when the wallet does not hold it. */
    void nft(String address, Callback<TONNFT> callback);

    /** Balance of one jetton, in its own smallest unit. */
    void jettonBalance(String jettonAddress, Callback<TonAmount> callback);

    /** This wallet's jetton wallet address for a jetton master. */
    void jettonWalletAddress(String jettonAddress, Callback<String> callback);

    /** Builds a jetton transfer. */
    void transferJettonTransaction(TONJettonsTransferRequest request, Callback<TONTransactionRequest> callback);

    /** Jettons this wallet holds. */
    void jettons(TONJettonsRequest request, Callback<TONJettonsResponse> callback);

}
