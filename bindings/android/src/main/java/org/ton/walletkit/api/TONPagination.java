//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

public class TONPagination {

    /** Builds one of these from JSON. */
    public static final Parser<TONPagination> PARSER = new Parser<TONPagination>() {
        @Override
        public TONPagination parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private Double limit;
    private Double offset;

    /** Maximum number of items to return */
    public Double getLimit() {
        return limit;
    }

    public TONPagination setLimit(Double value) {
        this.limit = value;
        return this;
    }

    /** Number of items to skip before starting to collect the result set */
    public Double getOffset() {
        return offset;
    }

    public TONPagination setOffset(Double value) {
        this.offset = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "limit", limit);
        TonApiJson.put(json, "offset", offset);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONPagination fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONPagination out = new TONPagination();
        out.limit = TonApiJson.optDouble(json, "limit");
        out.offset = TonApiJson.optDouble(json, "offset");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
