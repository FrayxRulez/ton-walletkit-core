//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/**
 * An asset the relayer accepts as fee payment.
 * Address shape is currently always a jetton master (TonAPI's only supported mode), but the type is intentionally generic so future relayers can advertise NFT items or other assets without re-shaping the model.
 */
public class TONGaslessSupportedAsset {

    /** Builds one of these from JSON. */
    public static final Parser<TONGaslessSupportedAsset> PARSER = new Parser<TONGaslessSupportedAsset>() {
        @Override
        public TONGaslessSupportedAsset parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String address;

    /**
     * Asset address — jetton master, NFT item, etc.
     * Required by the API.
     */
    public String getAddress() {
        return address;
    }

    public TONGaslessSupportedAsset setAddress(String value) {
        this.address = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "address", address);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONGaslessSupportedAsset fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONGaslessSupportedAsset out = new TONGaslessSupportedAsset();
        out.address = TonApiJson.optString(json, "address");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
