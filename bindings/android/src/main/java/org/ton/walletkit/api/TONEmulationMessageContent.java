//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Decoded content of an emulation message body. */
public class TONEmulationMessageContent {

    /** Builds one of these from JSON. */
    public static final Parser<TONEmulationMessageContent> PARSER = new Parser<TONEmulationMessageContent>() {
        @Override
        public TONEmulationMessageContent parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String body;
    private Object decoded;

    /** Message body in BOC base64 format, if available */
    public String getBody() {
        return body;
    }

    public TONEmulationMessageContent setBody(String value) {
        this.body = value;
        return this;
    }

    /** Structured decoded representation of the message body, if available */
    public Object getDecoded() {
        return decoded;
    }

    public TONEmulationMessageContent setDecoded(Object value) {
        this.decoded = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "body", body);
        TonApiJson.put(json, "decoded", decoded);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONEmulationMessageContent fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONEmulationMessageContent out = new TONEmulationMessageContent();
        out.body = TonApiJson.optString(json, "body");
        out.decoded = TonApiJson.opt(json, "decoded");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
