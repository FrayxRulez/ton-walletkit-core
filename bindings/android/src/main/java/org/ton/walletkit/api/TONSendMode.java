//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import java.util.List;

import org.json.JSONArray;
import org.json.JSONObject;

/** This mode determines how the message is sent, including whether to pay for gas separately and how to handle errors. */
public class TONSendMode {

    /** Builds one of these from JSON. */
    public static final Parser<TONSendMode> PARSER = new Parser<TONSendMode>() {
        @Override
        public TONSendMode parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private TONSendModeBase base;
    private List<TONSendModeFlag> flags;

    public TONSendModeBase getBase() {
        return base;
    }

    public TONSendMode setBase(TONSendModeBase value) {
        this.base = value;
        return this;
    }

    /** Required by the API. */
    public List<TONSendModeFlag> getFlags() {
        return flags;
    }

    public TONSendMode setFlags(List<TONSendModeFlag> value) {
        this.flags = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "base", base == null ? null : base.getValue());
        if (flags != null) {
            JSONArray array = new JSONArray();
            for (TONSendModeFlag item : flags) {
                array.put(item == null ? JSONObject.NULL : item.getValue());
            }
            TonApiJson.put(json, "flags", array);
        }
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONSendMode fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONSendMode out = new TONSendMode();
        out.base = TONSendModeBase.fromValue(TonApiJson.opt(json, "base"));
        out.flags = TonApiJson.enumList(TonApiJson.optArray(json, "flags"), TONSendModeFlag.VALUES);
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
