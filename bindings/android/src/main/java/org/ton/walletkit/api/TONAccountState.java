//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/**
 * Blockchain state of an account at a given point in time.
 * The `status` field distinguishes four cases:
 * - `active` — contract deployed, `code` and `data` present
 * - `uninitialized` — has balance/history but no contract deployed; `code`/`data` omitted
 * - `frozen` — frozen due to storage debt; `frozenHash` points at the pre-freeze state
 * - `non-existing` — no on-chain record at all; balance is `'0'` and other fields omitted
 */
public class TONAccountState {

    /** Builds one of these from JSON. */
    public static final Parser<TONAccountState> PARSER = new Parser<TONAccountState>() {
        @Override
        public TONAccountState parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String address;
    private String balance;
    private String code;
    private String data;
    private TONExtraCurrencies extraCurrencies;
    private TONTransactionId lastTransaction;
    private String rawBalance;
    private TONAccountStatus status;

    /**
     * Canonical (bounceable) representation of the account address.
     * Required by the API.
     */
    public String getAddress() {
        return address;
    }

    public TONAccountState setAddress(String value) {
        this.address = value;
        return this;
    }

    /**
     * Balance formatted in TON (10^9 nanotons = 1 TON).
     * Required by the API.
     */
    public String getBalance() {
        return balance;
    }

    public TONAccountState setBalance(String value) {
        this.balance = value;
        return this;
    }

    /** Base64-encoded contract code BOC. Omitted if the contract is not deployed. */
    public String getCode() {
        return code;
    }

    public TONAccountState setCode(String value) {
        this.code = value;
        return this;
    }

    /** Base64-encoded contract data BOC. Omitted if the contract is not deployed. */
    public String getData() {
        return data;
    }

    public TONAccountState setData(String value) {
        this.data = value;
        return this;
    }

    /**
     * Additional currencies attached to the account, keyed by currency id. Empty object if there are none.
     * Required by the API.
     */
    public TONExtraCurrencies getExtraCurrencies() {
        return extraCurrencies;
    }

    public TONAccountState setExtraCurrencies(TONExtraCurrencies value) {
        this.extraCurrencies = value;
        return this;
    }

    /** The most recent transaction applied to this account. Omitted if there have been no transactions (e.g. `non-existing` accounts). */
    public TONTransactionId getLastTransaction() {
        return lastTransaction;
    }

    public TONAccountState setLastTransaction(TONTransactionId value) {
        this.lastTransaction = value;
        return this;
    }

    /**
     * Balance in nanotons as a decimal string (raw on-chain amount).
     * Required by the API.
     */
    public String getRawBalance() {
        return rawBalance;
    }

    public TONAccountState setRawBalance(String value) {
        this.rawBalance = value;
        return this;
    }

    /**
     * On-chain account status: `active`, `uninitialized`, `frozen`, or `non-existing`.
     * Required by the API.
     */
    public TONAccountStatus getStatus() {
        return status;
    }

    public TONAccountState setStatus(TONAccountStatus value) {
        this.status = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "address", address);
        TonApiJson.put(json, "balance", balance);
        TonApiJson.put(json, "code", code);
        TonApiJson.put(json, "data", data);
        TonApiJson.put(json, "extraCurrencies", extraCurrencies == null ? null : extraCurrencies.toJson());
        TonApiJson.put(json, "lastTransaction", lastTransaction == null ? null : lastTransaction.toJson());
        TonApiJson.put(json, "rawBalance", rawBalance);
        TonApiJson.put(json, "status", status == null ? null : status.getValue());
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONAccountState fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONAccountState out = new TONAccountState();
        out.address = TonApiJson.optString(json, "address");
        out.balance = TonApiJson.optString(json, "balance");
        out.code = TonApiJson.optString(json, "code");
        out.data = TonApiJson.optString(json, "data");
        out.extraCurrencies = TONExtraCurrencies.fromJson(TonApiJson.optObject(json, "extraCurrencies"));
        out.lastTransaction = TONTransactionId.fromJson(TonApiJson.optObject(json, "lastTransaction"));
        out.rawBalance = TonApiJson.optString(json, "rawBalance");
        out.status = TONAccountStatus.fromValue(TonApiJson.opt(json, "status"));
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
