//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** TON blockchain network identifier. */
public class TONNetwork {

    /** Builds one of these from JSON. */
    public static final Parser<TONNetwork> PARSER = new Parser<TONNetwork>() {
        @Override
        public TONNetwork parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String chainId;

    /**
     * Chain ID of the network (e.g., "-239" for mainnet, "-3" for testnet)
     * Required by the API.
     */
    public String getChainId() {
        return chainId;
    }

    public TONNetwork setChainId(String value) {
        this.chainId = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "chainId", chainId);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONNetwork fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONNetwork out = new TONNetwork();
        out.chainId = TonApiJson.optString(json, "chainId");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
