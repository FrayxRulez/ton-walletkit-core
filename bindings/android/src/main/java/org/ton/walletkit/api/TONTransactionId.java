//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Canonical (lt, hash) pair identifying a transaction on the TON blockchain. */
public class TONTransactionId {

    /** Builds one of these from JSON. */
    public static final Parser<TONTransactionId> PARSER = new Parser<TONTransactionId>() {
        @Override
        public TONTransactionId parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String lt;

    /**
     * Logical time of the transaction as a decimal string. Uniquely orders transactions within an account.
     * Required by the API.
     */
    public String getLt() {
        return lt;
    }

    public TONTransactionId setLt(String value) {
        this.lt = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "lt", lt);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONTransactionId fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONTransactionId out = new TONTransactionId();
        out.lt = TonApiJson.optString(json, "lt");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
