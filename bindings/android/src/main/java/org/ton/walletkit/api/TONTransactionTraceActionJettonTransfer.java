//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

public class TONTransactionTraceActionJettonTransfer {

    /** Builds one of these from JSON. */
    public static final Parser<TONTransactionTraceActionJettonTransfer> PARSER = new Parser<TONTransactionTraceActionJettonTransfer>() {
        @Override
        public TONTransactionTraceActionJettonTransfer parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String amount;
    private String asset;
    private String destination;
    private String destinationJettonWallet;
    private String source;
    private String sourceJettonWallet;

    /** Amount of jettons transferred. */
    public String getAmount() {
        return amount;
    }

    public TONTransactionTraceActionJettonTransfer setAmount(String value) {
        this.amount = value;
        return this;
    }

    /** Jetton asset address being transferred. */
    public String getAsset() {
        return asset;
    }

    public TONTransactionTraceActionJettonTransfer setAsset(String value) {
        this.asset = value;
        return this;
    }

    /** Address of the receiver account. */
    public String getDestination() {
        return destination;
    }

    public TONTransactionTraceActionJettonTransfer setDestination(String value) {
        this.destination = value;
        return this;
    }

    /** Jetton wallet address of the receiver. */
    public String getDestinationJettonWallet() {
        return destinationJettonWallet;
    }

    public TONTransactionTraceActionJettonTransfer setDestinationJettonWallet(String value) {
        this.destinationJettonWallet = value;
        return this;
    }

    /** Address of the sender account. */
    public String getSource() {
        return source;
    }

    public TONTransactionTraceActionJettonTransfer setSource(String value) {
        this.source = value;
        return this;
    }

    /** Jetton wallet address of the sender. */
    public String getSourceJettonWallet() {
        return sourceJettonWallet;
    }

    public TONTransactionTraceActionJettonTransfer setSourceJettonWallet(String value) {
        this.sourceJettonWallet = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "amount", amount);
        TonApiJson.put(json, "asset", asset);
        TonApiJson.put(json, "destination", destination);
        TonApiJson.put(json, "destinationJettonWallet", destinationJettonWallet);
        TonApiJson.put(json, "source", source);
        TonApiJson.put(json, "sourceJettonWallet", sourceJettonWallet);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONTransactionTraceActionJettonTransfer fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONTransactionTraceActionJettonTransfer out = new TONTransactionTraceActionJettonTransfer();
        out.amount = TonApiJson.optString(json, "amount");
        out.asset = TonApiJson.optString(json, "asset");
        out.destination = TonApiJson.optString(json, "destination");
        out.destinationJettonWallet = TonApiJson.optString(json, "destinationJettonWallet");
        out.source = TonApiJson.optString(json, "source");
        out.sourceJettonWallet = TonApiJson.optString(json, "sourceJettonWallet");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
