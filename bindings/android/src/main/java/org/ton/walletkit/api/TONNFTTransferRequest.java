//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Request parameters for transferring an NFT to another address. */
public class TONNFTTransferRequest {

    /** Builds one of these from JSON. */
    public static final Parser<TONNFTTransferRequest> PARSER = new Parser<TONNFTTransferRequest>() {
        @Override
        public TONNFTTransferRequest parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String comment;
    private String nftAddress;
    private String recipientAddress;
    private String transferAmount;

    /** Human-readable comment attached to the transfer */
    public String getComment() {
        return comment;
    }

    public TONNFTTransferRequest setComment(String value) {
        this.comment = value;
        return this;
    }

    /**
     * Contract address of the NFT to transfer
     * Required by the API.
     */
    public String getNftAddress() {
        return nftAddress;
    }

    public TONNFTTransferRequest setNftAddress(String value) {
        this.nftAddress = value;
        return this;
    }

    /**
     * Recipient wallet address
     * Required by the API.
     */
    public String getRecipientAddress() {
        return recipientAddress;
    }

    public TONNFTTransferRequest setRecipientAddress(String value) {
        this.recipientAddress = value;
        return this;
    }

    /** TON amount to attach for gas fees (default: 0.1 TON) */
    public String getTransferAmount() {
        return transferAmount;
    }

    public TONNFTTransferRequest setTransferAmount(String value) {
        this.transferAmount = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "comment", comment);
        TonApiJson.put(json, "nftAddress", nftAddress);
        TonApiJson.put(json, "recipientAddress", recipientAddress);
        TonApiJson.put(json, "transferAmount", transferAmount);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONNFTTransferRequest fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONNFTTransferRequest out = new TONNFTTransferRequest();
        out.comment = TonApiJson.optString(json, "comment");
        out.nftAddress = TonApiJson.optString(json, "nftAddress");
        out.recipientAddress = TonApiJson.optString(json, "recipientAddress");
        out.transferAmount = TonApiJson.optString(json, "transferAmount");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
