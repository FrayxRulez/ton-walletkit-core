//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Parameters for getting a staking quote */
public class TONStakingQuoteParams {

    /** Builds one of these from JSON. */
    public static final Parser<TONStakingQuoteParams> PARSER = new Parser<TONStakingQuoteParams>() {
        @Override
        public TONStakingQuoteParams parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String amount;
    private TONStakingQuoteDirection direction;
    private Boolean isReversed;
    private TONNetwork network;
    private Object providerOptions;
    private TONUnstakeModes unstakeMode;
    private String userAddress;

    /**
     * Amount of tokens to stake or unstake
     * Required by the API.
     */
    public String getAmount() {
        return amount;
    }

    public TONStakingQuoteParams setAmount(String value) {
        this.amount = value;
        return this;
    }

    /**
     * Direction of the quote (stake or unstake)
     * Required by the API.
     */
    public TONStakingQuoteDirection getDirection() {
        return direction;
    }

    public TONStakingQuoteParams setDirection(TONStakingQuoteDirection value) {
        this.direction = value;
        return this;
    }

    /** If true, for unstake requests the amount is specified in the staking coin (e.g. TON) instead of the Liquid Staking Token (e.g. tsTON). */
    public Boolean getIsReversed() {
        return isReversed;
    }

    public TONStakingQuoteParams setIsReversed(Boolean value) {
        this.isReversed = value;
        return this;
    }

    /** Network on which the staking will be executed */
    public TONNetwork getNetwork() {
        return network;
    }

    public TONStakingQuoteParams setNetwork(TONNetwork value) {
        this.network = value;
        return this;
    }

    /** Provider-specific options */
    public Object getProviderOptions() {
        return providerOptions;
    }

    public TONStakingQuoteParams setProviderOptions(Object value) {
        this.providerOptions = value;
        return this;
    }

    /** Requested mode of unstaking */
    public TONUnstakeModes getUnstakeMode() {
        return unstakeMode;
    }

    public TONStakingQuoteParams setUnstakeMode(TONUnstakeModes value) {
        this.unstakeMode = value;
        return this;
    }

    /** Address of the user */
    public String getUserAddress() {
        return userAddress;
    }

    public TONStakingQuoteParams setUserAddress(String value) {
        this.userAddress = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "amount", amount);
        TonApiJson.put(json, "direction", direction == null ? null : direction.getValue());
        TonApiJson.put(json, "isReversed", isReversed);
        TonApiJson.put(json, "network", network == null ? null : network.toJson());
        TonApiJson.put(json, "providerOptions", providerOptions);
        TonApiJson.put(json, "unstakeMode", unstakeMode == null ? null : unstakeMode.getValue());
        TonApiJson.put(json, "userAddress", userAddress);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONStakingQuoteParams fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONStakingQuoteParams out = new TONStakingQuoteParams();
        out.amount = TonApiJson.optString(json, "amount");
        out.direction = TONStakingQuoteDirection.fromValue(TonApiJson.opt(json, "direction"));
        out.isReversed = TonApiJson.optBoolean(json, "isReversed");
        out.network = TONNetwork.fromJson(TonApiJson.optObject(json, "network"));
        out.providerOptions = TonApiJson.opt(json, "providerOptions");
        out.unstakeMode = TONUnstakeModes.fromValue(TonApiJson.opt(json, "unstakeMode"));
        out.userAddress = TonApiJson.optString(json, "userAddress");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
