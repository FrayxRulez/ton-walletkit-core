//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Attribute/trait of an NFT, commonly used for rarity and properties. */
public class TONNFTAttribute {

    /** Builds one of these from JSON. */
    public static final Parser<TONNFTAttribute> PARSER = new Parser<TONNFTAttribute>() {
        @Override
        public TONNFTAttribute parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String displayType;
    private String traitType;
    private String value;

    /** How the attribute should be displayed (e.g., "string", "number", "date") */
    public String getDisplayType() {
        return displayType;
    }

    public TONNFTAttribute setDisplayType(String value) {
        this.displayType = value;
        return this;
    }

    /** Category or type of the trait (e.g., "Background", "Eyes") */
    public String getTraitType() {
        return traitType;
    }

    public TONNFTAttribute setTraitType(String value) {
        this.traitType = value;
        return this;
    }

    /** Value of the attribute (e.g., "Blue", "Rare") */
    public String getValue() {
        return value;
    }

    public TONNFTAttribute setValue(String value) {
        this.value = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "displayType", displayType);
        TonApiJson.put(json, "traitType", traitType);
        TonApiJson.put(json, "value", value);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONNFTAttribute fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONNFTAttribute out = new TONNFTAttribute();
        out.displayType = TonApiJson.optString(json, "displayType");
        out.traitType = TonApiJson.optString(json, "traitType");
        out.value = TonApiJson.optString(json, "value");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
