//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import java.util.List;

import org.json.JSONArray;
import org.json.JSONObject;

/** Node in the transaction execution tree. */
public class TONTransactionTraceNode {

    /** Builds one of these from JSON. */
    public static final Parser<TONTransactionTraceNode> PARSER = new Parser<TONTransactionTraceNode>() {
        @Override
        public TONTransactionTraceNode parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private List<TONTransactionTraceNode> children;

    /**
     * Child trace nodes representing spawned messages
     * Required by the API.
     */
    public List<TONTransactionTraceNode> getChildren() {
        return children;
    }

    public TONTransactionTraceNode setChildren(List<TONTransactionTraceNode> value) {
        this.children = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        if (children != null) {
            JSONArray array = new JSONArray();
            for (TONTransactionTraceNode item : children) {
                array.put(item == null ? JSONObject.NULL : item.toJson());
            }
            TonApiJson.put(json, "children", array);
        }
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONTransactionTraceNode fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONTransactionTraceNode out = new TONTransactionTraceNode();
        out.children = TonApiJson.list(TonApiJson.optArray(json, "children"), TONTransactionTraceNode.PARSER);
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
