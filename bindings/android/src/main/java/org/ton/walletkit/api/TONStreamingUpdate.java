//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** A union type representing all possible data updates from a streaming provider. */
/**
 * A union: {@code type} says which variant this is, and the matching
 * accessor returns it. An unrecognised type leaves the value as the raw
 * JSON rather than failing, so a new variant upstream does not break a
 * response that merely contains one.
 */
public class TONStreamingUpdate {

    public static final Parser<TONStreamingUpdate> PARSER = new Parser<TONStreamingUpdate>() {
        @Override
        public TONStreamingUpdate parse(JSONObject json) {
            return fromJson(json);
        }
    };

    public static final String BALANCE = "balance";
    public static final String TRANSACTIONS = "transactions";
    public static final String JETTONS = "jettons";

    private String type;
    private Object value;

    /** Which variant this is; one of the constants above. */
    public String getType() {
        return type;
    }

    /** The variant, typed by {@code type}, or the raw JSON if unrecognised. */
    public Object getValue() {
        return value;
    }

    /** The value when {@code type} is {@code balance}, else null. */
    public TONBalanceUpdate asBalance() {
        return value instanceof TONBalanceUpdate ? (TONBalanceUpdate) value : null;
    }

    /** The value when {@code type} is {@code transactions}, else null. */
    public TONTransactionsUpdate asTransactions() {
        return value instanceof TONTransactionsUpdate ? (TONTransactionsUpdate) value : null;
    }

    /** The value when {@code type} is {@code jettons}, else null. */
    public TONJettonUpdate asJettons() {
        return value instanceof TONJettonUpdate ? (TONJettonUpdate) value : null;
    }

    public JSONObject toJson() {
        Object encoded = asJson(value);
        JSONObject json = encoded instanceof JSONObject ? (JSONObject) encoded : new JSONObject();
        TonApiJson.put(json, "type", type);
        return json;
    }

    private static Object asJson(Object value) {
        if (value instanceof TONBalanceUpdate) {
            return ((TONBalanceUpdate) value).toJson();
        }
        if (value instanceof TONTransactionsUpdate) {
            return ((TONTransactionsUpdate) value).toJson();
        }
        if (value instanceof TONJettonUpdate) {
            return ((TONJettonUpdate) value).toJson();
        }
        return value;
    }

    public static TONStreamingUpdate fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONStreamingUpdate out = new TONStreamingUpdate();
        out.type = TonApiJson.optString(json, "type");
        JSONObject payload = json;
        if (BALANCE.equals(out.type)) {
            out.value = TONBalanceUpdate.fromJson(payload);
        } else if (TRANSACTIONS.equals(out.type)) {
            out.value = TONTransactionsUpdate.fromJson(payload);
        } else if (JETTONS.equals(out.type)) {
            out.value = TONJettonUpdate.fromJson(payload);
        } else {
            out.value = payload;
        }
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
