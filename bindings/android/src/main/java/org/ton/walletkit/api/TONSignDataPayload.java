//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Payload structure for prepared sign data. */
public class TONSignDataPayload {

    /** Builds one of these from JSON. */
    public static final Parser<TONSignDataPayload> PARSER = new Parser<TONSignDataPayload>() {
        @Override
        public TONSignDataPayload parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private TONSignData data;
    private String fromAddress;
    private TONNetwork network;

    /**
     * Sign data content to be signed
     * Required by the API.
     */
    public TONSignData getData() {
        return data;
    }

    public TONSignDataPayload setData(TONSignData value) {
        this.data = value;
        return this;
    }

    /** Optional sender address in received format (raw, user friendly) */
    public String getFromAddress() {
        return fromAddress;
    }

    public TONSignDataPayload setFromAddress(String value) {
        this.fromAddress = value;
        return this;
    }

    /** Network where the signing will occur */
    public TONNetwork getNetwork() {
        return network;
    }

    public TONSignDataPayload setNetwork(TONNetwork value) {
        this.network = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "data", data == null ? null : data.toJson());
        TonApiJson.put(json, "fromAddress", fromAddress);
        TonApiJson.put(json, "network", network == null ? null : network.toJson());
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONSignDataPayload fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONSignDataPayload out = new TONSignDataPayload();
        out.data = TONSignData.fromJson(TonApiJson.optObject(json, "data"));
        out.fromAddress = TonApiJson.optString(json, "fromAddress");
        out.network = TONNetwork.fromJson(TonApiJson.optObject(json, "network"));
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
