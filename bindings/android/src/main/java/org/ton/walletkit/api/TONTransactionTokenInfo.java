//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import org.json.JSONObject;

/** Token information discriminated by token type. */
/**
 * A union: {@code type} says which variant this is, and the matching
 * accessor returns it. An unrecognised type leaves the value as the raw
 * JSON rather than failing, so a new variant upstream does not break a
 * response that merely contains one.
 */
public class TONTransactionTokenInfo {

    public static final Parser<TONTransactionTokenInfo> PARSER = new Parser<TONTransactionTokenInfo>() {
        @Override
        public TONTransactionTokenInfo parse(JSONObject json) {
            return fromJson(json);
        }
    };

    public static final String JETTON_WALLETS = "jetton_wallets";
    public static final String JETTON_MASTERS = "jetton_masters";
    public static final String UNKNOWN = "unknown";

    private String type;
    private Object value;

    /** Which variant this is; one of the constants above. */
    public String getType() {
        return type;
    }

    /** The variant, typed by {@code type}, or the raw JSON if unrecognised. */
    public Object getValue() {
        return value;
    }

    /** The value when {@code type} is {@code jetton_wallets}, else null. */
    public TONTransactionTokenInfoJettonWallets asJettonWallets() {
        return value instanceof TONTransactionTokenInfoJettonWallets ? (TONTransactionTokenInfoJettonWallets) value : null;
    }

    /** The value when {@code type} is {@code jetton_masters}, else null. */
    public TONTransactionTokenInfoJettonMasters asJettonMasters() {
        return value instanceof TONTransactionTokenInfoJettonMasters ? (TONTransactionTokenInfoJettonMasters) value : null;
    }

    /** The value when {@code type} is {@code unknown}, else null. */
    public TONTransactionTokenInfoBase asUnknown() {
        return value instanceof TONTransactionTokenInfoBase ? (TONTransactionTokenInfoBase) value : null;
    }

    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "type", type);
        TonApiJson.put(json, "value", asJson(value));
        return json;
    }

    private static Object asJson(Object value) {
        if (value instanceof TONTransactionTokenInfoJettonWallets) {
            return ((TONTransactionTokenInfoJettonWallets) value).toJson();
        }
        if (value instanceof TONTransactionTokenInfoJettonMasters) {
            return ((TONTransactionTokenInfoJettonMasters) value).toJson();
        }
        if (value instanceof TONTransactionTokenInfoBase) {
            return ((TONTransactionTokenInfoBase) value).toJson();
        }
        return value;
    }

    public static TONTransactionTokenInfo fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONTransactionTokenInfo out = new TONTransactionTokenInfo();
        out.type = TonApiJson.optString(json, "type");
        JSONObject payload = TonApiJson.optObject(json, "value");
        if (JETTON_WALLETS.equals(out.type)) {
            out.value = TONTransactionTokenInfoJettonWallets.fromJson(payload);
        } else if (JETTON_MASTERS.equals(out.type)) {
            out.value = TONTransactionTokenInfoJettonMasters.fromJson(payload);
        } else if (UNKNOWN.equals(out.type)) {
            out.value = TONTransactionTokenInfoBase.fromJson(payload);
        } else {
            out.value = payload;
        }
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
