//
// Copyright (c) Fela Ameghino 2026
//
// Distributed under the MIT License. (See accompanying file LICENSE or copy at
// https://opensource.org/licenses/MIT)
//
// GENERATED by scripts/generate-api/emit-java-dto.mjs from the walletkit types —
// do not edit. Run `npm run java` in scripts/generate-api to regenerate.
//

package org.ton.walletkit.api;

import java.util.List;

import org.json.JSONArray;
import org.json.JSONObject;

/**
 * Quote for a gasless transaction produced by `GaslessProvider.getQuote`.
 * Contains relayer-wrapped messages that should be passed to `wallet.signMessage` in place of the caller's original messages, together with the fee the relayer will deduct and the timestamp after which the bundle expires.
 */
public class TONGaslessQuote {

    /** Builds one of these from JSON. */
    public static final Parser<TONGaslessQuote> PARSER = new Parser<TONGaslessQuote>() {
        @Override
        public TONGaslessQuote parse(JSONObject json) {
            return fromJson(json);
        }
    };

    private String fee;
    private String from;
    private List<TONTransactionRequestMessage> messages;
    private TONNetwork network;
    private Double validUntil;

    /**
     * Relayer fee in fee-jetton nanounits
     * Required by the API.
     */
    public String getFee() {
        return fee;
    }

    public TONGaslessQuote setFee(String value) {
        this.fee = value;
        return this;
    }

    /**
     * Sender wallet address echoed by the relayer
     * Required by the API.
     */
    public String getFrom() {
        return from;
    }

    public TONGaslessQuote setFrom(String value) {
        this.from = value;
        return this;
    }

    /**
     * Relayer-wrapped messages ready to be signed
     * Required by the API.
     */
    public List<TONTransactionRequestMessage> getMessages() {
        return messages;
    }

    public TONGaslessQuote setMessages(List<TONTransactionRequestMessage> value) {
        this.messages = value;
        return this;
    }

    /**
     * Network the quote is bound to (mirrors the request `network`)
     * Required by the API.
     */
    public TONNetwork getNetwork() {
        return network;
    }

    public TONGaslessQuote setNetwork(TONNetwork value) {
        this.network = value;
        return this;
    }

    /**
     * Unix timestamp after which the bundle becomes invalid for relay
     * Required by the API.
     */
    public Double getValidUntil() {
        return validUntil;
    }

    public TONGaslessQuote setValidUntil(Double value) {
        this.validUntil = value;
        return this;
    }

    /** This model as JSON. Null members are omitted, not written as null. */
    public JSONObject toJson() {
        JSONObject json = new JSONObject();
        TonApiJson.put(json, "fee", fee);
        TonApiJson.put(json, "from", from);
        if (messages != null) {
            JSONArray array = new JSONArray();
            for (TONTransactionRequestMessage item : messages) {
                array.put(item == null ? JSONObject.NULL : item.toJson());
            }
            TonApiJson.put(json, "messages", array);
        }
        TonApiJson.put(json, "network", network == null ? null : network.toJson());
        TonApiJson.put(json, "validUntil", validUntil);
        return json;
    }

    /** @return null when {@code json} is null. */
    public static TONGaslessQuote fromJson(JSONObject json) {
        if (json == null) {
            return null;
        }
        TONGaslessQuote out = new TONGaslessQuote();
        out.fee = TonApiJson.optString(json, "fee");
        out.from = TonApiJson.optString(json, "from");
        out.messages = TonApiJson.list(TonApiJson.optArray(json, "messages"), TONTransactionRequestMessage.PARSER);
        out.network = TONNetwork.fromJson(TonApiJson.optObject(json, "network"));
        out.validUntil = TonApiJson.optDouble(json, "validUntil");
        return out;
    }

    @Override
    public String toString() {
        return toJson().toString();
    }
}
