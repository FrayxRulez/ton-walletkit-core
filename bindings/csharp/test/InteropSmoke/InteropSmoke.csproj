<Project Sdk="Microsoft.NET.Sdk">
  <!-- Dev-only harness: exercises the binding against a real twk.dll. The shipped
       library targets netstandard2.0; this runs it on the desktop runtime. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>disable</Nullable>
    <AssemblyName>InteropSmoke</AssemblyName>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\Ton.WalletKit.csproj" />
  </ItemGroup>
</Project>
