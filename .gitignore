# build output
/build/
/build-*/
/out/
/cmake-build-*/
compile_commands.json
.cache/

# native artifacts
*.o
*.obj
*.a
*.lib
*.so
*.dylib
*.dll
*.exp
*.ilk
*.pdb

# js bundle build
/js/node_modules/
/js/dist/
/js/.esbuild/

# codegen: node_modules, the downloaded generator jar, and the staging output.
# Only the generated C# under bindings/ is committed.
/scripts/generate-api/node_modules/
/scripts/generate-api/build/

# editors / os
.DS_Store
.vs/
.vscode/
.idea/

# C# build output
bin/
obj/

# upstream reference clones (scripts/upstream.mjs)
/.upstream/
build-java/
build-android*/
build-host/
build-jni/
build-clean/
build-bc/
/smoke/

# Android binding: the .so files are build output staged for packaging, and the
# Gradle build is a normal Gradle build.
/bindings/android/src/main/jniLibs/
/bindings/android/build/
/bindings/android/.gradle/
/bindings/android/local.properties
