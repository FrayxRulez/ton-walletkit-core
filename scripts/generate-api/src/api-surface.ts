// The DTO surface to generate from: walletkit's own api/models barrel, which is
// exactly what kit-android generates its Kotlin models from.
export type * from '@ton/walletkit/dist/esm/api/models/index';
