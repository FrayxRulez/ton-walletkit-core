{
  "name": "twk-generate-api",
  "private": true,
  "description": "Regenerates the C# DTOs from @ton/walletkit's TypeScript types, mirroring kit-android's codegen so the bindings stay in sync with upstream.",
  "scripts": {
    "surface": "node build-surface.mjs",
    "schema": "ts-json-schema-generator --path src/api-surface.ts --tsconfig tsconfig.json --type \"*\" --expose all --no-top-ref --out build/schema.json",
    "openapi": "node schema-to-openapi.mjs",
    "csharp": "node run-openapi-generator.mjs",
    "java": "node emit-java-dto.mjs",
    "build": "npm run surface && npm run schema && npm run openapi && npm run csharp && npm run java"
  },
  "devDependencies": {
    "ts-json-schema-generator": "2.4.0",
    "typescript": "5.9.3"
  }
}
